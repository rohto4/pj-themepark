# P6 functional closure evidence — 2026-08-01 JST

## Scope

This checkpoint covers P6-A through P6-F: the Bloomworks root instrument, visible park-wide
Afterlight, five-act Constellary, and local return-night continuity. It does not record a P6
production deployment; the public Worker remains on the stable P0–P5 source.

## Observed automated gate

Command: `npm.cmd run check` followed by `npm.cmd run check:assets`.

- formatting: pass;
- ESLint: pass;
- TypeScript project build: pass;
- Vitest: 19 files / 114 tests pass;
- Vite production build: pass;
- Playwright: 22/22 Chromium routes across 1440×900 desktop and Pixel 7 mobile projects pass;
- asset budget: 11 files / 454.93 KiB pass.

The browser routes now include active five-act conducting, watch-only playback, reduced-motion and
low-power still equivalence, Bloomworks three-root and six-root routes, downstream geometry, and a
full local return-night route with reload.

## Return-night observation

The repeatable route uses only public UI:

1. Finish a bridge Bloom pattern with one moon root, then two other realms and the finale.
2. Begin another local night and reload the page.
3. Observe that attraction completion and traces are empty while preferences remain.
4. Enter Bloomworks and see a compressed bridge afterimage before placing a new root.
5. Use the one listening Crossing socket to answer with a different gear.
6. Observe a new `memory` path on the map and five-act Constellary recognition geometry.

Desktop and mobile both passed in reduced-motion plus low-power mode. The afterimage had zero
running animations, and the final scene had no measured horizontal overflow. Internal carry tokens
were absent from the guest-facing motif ledger; authored `memory:bloom:*` and `dawn-root:*` motifs
were present.

## Interaction-density audit

The return fixture performs approximately 43 deliberate activations over two nights. The current
deep single-night inventory includes:

- a three-placement Bloomworks core with immediate graph growth and a safe exit;
- three optional spatial moon roots, strategy divergence, and a six-root chorus secret;
- alternate attraction replays and cross-realm callbacks;
- four distinct realm instruments plus three unscored Hushgarden details;
- active five-act conducting or equivalent watch/still routes;
- a subsequent-night afterimage and one expressive reply that changes later world geometry.

The automated return fixture completed in roughly 1.8–2.3 seconds because automation removes human
reading, exploration, hesitation, listening, and self-directed replay. This proves reachability and
interaction structure, not a literal 30–60 minute visit. No wait, click-rate multiplication, or
branch-count estimate is treated as dwell-time evidence.

## Remaining evidence before release

- Batched observed visual QA at representative desktop, tablet, and mobile widths.
- Human listening-quality judgment for the generative score remains unavailable.
- A literal 30–60 minute human dwell observation remains unavailable; P6 can document that gap but
  must not relabel automation as duration evidence.
- Full local gate must be repeated after visual fixes, then the Cloudflare deployment, live route,
  rollback record, and independent audit must be updated separately.
